****

cmd:

```bash
usage: dal [options] <command> [args]

Options:
    --base=<path to base dir>
        Select base dir to work
    --base-working-dir=<path to descendant>
        Find base dir from its descendant up

Command:
    init
    pull
    push
    cmd
  

```
